#pragma once

// #include "core/context-manager.h"

namespace Engine
{
  class Renderer
  {
  public:
    Renderer();

    // private:
    // SystemCore::ContextManager contextManager;
    // uint32_t lastRender = 0;
  };
}